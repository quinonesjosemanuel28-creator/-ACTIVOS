node_modules
dist
dist-ssr
*.local
.DS_Store

# Secrets — la API key NO va a git (.env.example sí, como plantilla)
.env
.env.*
!.env.example

# Base de datos local (se regenera con `npm run seed`)
data/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Logs / temporales
*.log
.tmp
